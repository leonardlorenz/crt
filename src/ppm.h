#include "image.h"

#ifndef PPM_H
#define PPM_H

/** writes an image to given path in PPM format **/
void write_ppm(Image img);

/** clears content of current line and fills the array with zeroes **/
void write_ppm(Image img);

#endif
